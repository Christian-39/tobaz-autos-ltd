# Django Settings
DJANGO_SECRET_KEY=your-secret-key-here-change-in-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Database Settings (MySQL)
DB_NAME=tobaz_autos
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306

# Set to True to use SQLite instead of MySQL
USE_SQLITE=False
